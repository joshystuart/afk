port: 3001
nodeEnv: development
baseUrl: http://localhost

logger:
  level: debug
  prettyPrint: true

session:
  maxSessionsPerUser: 10

adminUser:
  username: "${ADMIN_USERNAME:-admin}"
  password: "${ADMIN_PASSWORD:-password123}"

auth:
  jwtSecret: "${JWT_SECRET:-afk-development-secret-key-change-in-production}"

database:
  type: "${DATABASE_TYPE:-sqlite}"
  sqlite:
    database: "${DB_DATABASE:-afk.sqlite}"
    synchronize: true
    logging: true
  postgres:
    host: "${DB_HOST:-localhost}"
    port: "${DB_PORT:-5432}"
    username: "${DB_USERNAME:-afk}"
    password: "${DB_PASSWORD:-password}"
    database: "${DB_NAME:-afk}"
    synchronize: "${DB_SYNCHRONIZE:-true}"
    logging: "${DB_LOGGING:-true}"