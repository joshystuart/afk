port: 3001
nodeEnv: test
baseUrl: http://localhost

logger:
  level: silent
  prettyPrint: false

session:
  maxSessionsPerUser: 10

adminUser:
  username: admin
  password: admin123

auth:
  jwtSecret: "afk-development-secret-key-change-in-production"

database:
  type: "sqlite"
  sqlite:
    database: "afk-test.sqlite"
    synchronize: true
    logging: false
  postgres:
    host: "localhost"
    port: 5432
    username: "afk"
    password: "password"
    database: "afk_test"
    synchronize: true
    logging: false